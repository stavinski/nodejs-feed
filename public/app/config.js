define(function() {
    
    config = {};
    config.api = {};
    config.api.baseUrl = '/api/';
    
    config.toastr = {};
    config.toastr.options = {
        positionClass : 'toast-bottom-right'
    };
    
    return config;
    
});