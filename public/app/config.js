define(function() {
    
    config = {};
    config.api = {};
    config.api.baseUri = '/api/';
    
    config.toastr = {};
    config.toastr.options = {
        positionClass : 'toast-bottom-right'
    };
    
    return config;
    
});